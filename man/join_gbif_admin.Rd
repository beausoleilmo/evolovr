% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_gbif.R
\name{join_gbif_admin}
\alias{join_gbif_admin}
\title{Prépare et jointure des données GBIF et
des régions administratives via la grille H3}
\usage{
join_gbif_admin(
  con = NULL,
  config,
  res = 10L,
  basisRec = c("HUMAN_OBSERVATION", "MACHINE_OBSERVATION"),
  taxRank = c("SPECIES", "SUBSPECIES", "VARIETY"),
  kingdm = c("Chromista", "Fungi", "Plantae", "Animalia"),
  coordUncertainM = 200
)
}
\arguments{
\item{con}{Connexion DuckDB valide. Si \code{NULL}, une
connexion temporaire est initialisée.}

\item{config}{Liste avec les chemins d'accès au minimum :
\itemize{
\item \code{gbif_raw} : fichier GBIF d'entrée en .parquet préalablement
transformé après le téléchargement avec \code{\link[=transforme_gbif]{transforme_gbif()}}.
\item \code{out_admin_pq} : tableau des noms administrative et leur index
H3 à une résolution.
\item \code{out_gbif_pq} : chemin d'accès de sortie (\code{./path/nom.parquet}).
}}

\item{res}{Integer. Résolution de la grille H3 (ex: \code{10L}).}

\item{basisRec}{Vecteur avec basisOfRecord}

\item{taxRank}{Vecteur avec taxonRank}

\item{kingdm}{Vecteur avec kingdom}

\item{coordUncertainM}{Vecteur avec coordinateUncertaintyInMeters}
}
\value{
NULL. La fonction exporte directement le
fichier traité sur le disque.
}
\description{
Filtre les données brutes GBIF, calcule l'indexation
spatiale H3 à la résolution spécifiée, puis effectue une jointure
interne avec une table administrative précalculée.
Le résultat est exporté directement en Parquet compressé.
}
\details{
L'identifiant H3 retourné est un entier DuckDB de type \code{uint64}.
Comme R ne gère pas nativement le 64-bit de manière précise,
cet ID est traité sous forme de chaîne de
caractères ou via le type natif de DuckDB lors des requêtes.
}
\examples{
\dontrun{
con <- setup_duckdb()
paths <- list(
  gbif_raw     = "data/gbif_prep_all.parquet",
  out_admin_pq = "data/admin_mun.parquet",
  out_gbif_pq  = "data/gbif_prep_h3_res_10.parquet"
)
join_gbif_admin(con, config = paths, res = 10L)
}
}
